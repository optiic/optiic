const assert = require('assert');

beforeEach(() => {
});

before(() => {
});

after(() => {
});

/*
 * ============
 *  Test Cases
 * ============
 */
describe('Sniips', () => {
  const lib = require('../dist/index.js');

  // Method
  describe('.method()', () => {

    // Method tests
    it('this => that', () => {
      return assert.equal(lib, {});
    });

  });

})
